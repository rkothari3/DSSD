[project]
name = "dssd"
version = "0.1.0"
description = "Fault-tolerant membership: SWIM failure detection + Raft leader election"
requires-python = ">=3.11"
dependencies = [
    "grpcio>=1.66",
    "protobuf>=5.27",
    "torch>=2.0",
    "numpy>=1.26",
    "aiohttp>=3.9",
]

[project.scripts]
dssd-member = "dssd.cmd.member:main"
dssd-worker = "dssd.diloco.worker:main"
dssd-dashboard = "dssd.dashboard.server:main"
dssd-region = "dssd.sharding.cli:main"
dssd-experiment = "dssd.diloco.experiment:main"

[project.optional-dependencies]
dev = [
    "grpcio-tools>=1.66",
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
    "ruff>=0.6",
    "mypy>=1.11",
    "types-protobuf",
    "types-grpcio",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"dssd.dashboard" = ["static/*.html"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
extend-exclude = ["*_pb2.py", "*_pb2.pyi", "*_pb2_grpc.py"]

[tool.mypy]
exclude = ["_pb2(_grpc)?\\.py$", "_pb2\\.pyi$"]
